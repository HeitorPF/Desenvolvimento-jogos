using UnityEngine;

public class rangeAtk1FinalBoss : controladorDano
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        dano = 10;
        atacante = "inimigo";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
