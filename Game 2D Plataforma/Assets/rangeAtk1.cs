using UnityEngine;

public class rangeAtk1 : controladorDano
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        dano = 4;
        atacante = "inimigo";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
